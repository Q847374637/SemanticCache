# SemanticCache.Ollama

The official local Ollama embedding provider for `SemanticCache.Core`. Run high-performance semantic caching entirely locally without API costs using models like `nomic-embed-text`.

For full documentation, setup instructions, and dependency injection examples, please visit the [Official GitHub Repository](https://github.com/Q847374637/SemanticCache).